version: '2'
services:
    web:
        build: .
        command: python manage.py runserver 0.0.0.0:8990
        volumes:
            - .:/code
        ports:
            - "8990:8990"
        depends_on:
            - db
    db:
        image: postgres